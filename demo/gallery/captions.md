# Gallery captions

Paste the Caption column, and nothing else: the plate already carries its own label, and the field rejects anything past about 140 characters rather than trimming it. Almost every one is the same string that is burned into the picture; only 14 and 15 needed a shorter wording. Upload in this order; 13 to 15 come from [diagrams.html](../diagrams.html), screenshotted at 1600x1067.

| # | Plate | Len | Caption |
|---:|---|---:|---|
| 01 | THE PLAN | 130 | Fourteen courses, put there by hand. Term 3 has six credits left, and the page has registered its thirteen tools with the browser. |
| 02 | THE QUESTION | 102 | The student asks in their own words, naming no tool: what are my options, and what does each one cost? |
| 03 | compare_options | 105 | Both futures in one call. Either choice closes a specialisation — the slot is what costs, not the course. |
| 04 | protect_track | 125 | The student draws a line. Anything that would close Graphics is refused from now on, including if they ask for it themselves. |
| 05 | THE LIMIT, ON THE PLAN | 138 | A lock beside the track, and a line naming who it binds. Every other rule here belongs to the university. This one belongs to the student. |
| 06 | REFUSED | 108 | The agent asks for NUM-201. The page refuses, citing the student's own instruction, and names both ways out. |
| 07 | WHAT THE ASSISTANT SAW | 104 | The refusal comes back as prose, so the agent reports it rather than retrying. Nothing instructed it to. |
| 08 | REWIND | 129 | Every call is an event and the plan is their reduction, so undo is replaying fewer of them. The protection unwinds with the rest. |
| 09 | THIRTEEN TOOLS | 131 | Each description says when to reach for it. That string is the only thing a model has to choose on, so the strings are the product. |
| 10 | REGISTRATION | 126 | registerAll, on document.modelContext. No build step, no dependencies, no server: what is in the repository is what is served. |
| 11 | WHO CALLED | 136 | The page counts calls by origin and states plainly that it cannot verify the second number. WebMCP gives the handler no caller identity. |
| 12 | LIVE |  84 | Apache-2.0, thirteen tools on document.modelContext, and no server behind any of it. |
| 13 | THE MODULES | 135 | Eleven files, no build step and no dependencies. Every import points downward, which is the whole reason the layering is worth drawing. |
| 14 | THE ONE DECISION | 114 | Every tool call is an event, and the plan is their reduction. Refusing, undoing and auditing all fall out of that. |
| 15 | WHERE IT WAS TESTED |  78 | Both environments named in the challenge rules, and both make real tool calls. |
